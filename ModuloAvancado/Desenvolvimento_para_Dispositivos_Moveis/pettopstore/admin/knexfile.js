module.exports = {
  client: 'mysql2',
  connection: {
    host : '127.0.0.1',
    user : 'root',         // <-- Seu usuário do MySQL (geralmente root)
    password : 'luiz12345', // <-- Coloque a sua senha do MySQL aqui
    database : 'pettopstore'
  }
};